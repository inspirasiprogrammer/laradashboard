interface:
  display_name: "Laradashboard"
  short_description: "Repo-specific patterns and workflows for laradashboard"
  default_prompt: "Use the laradashboard repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true